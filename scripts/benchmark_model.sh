#!/usr/bin/env bash
# PLACEHOLDER — implemented in Phase 1 (ENGINEERING_PLAN.md §5).
#
# Phase 1 responsibilities: record VRAM (idle / after load / 8K prompt / 32K prompt /
# target context), prompt tok/s, generation tok/s and time-to-first-token.
set -euo pipefail

echo "benchmark_model.sh is a Phase 0 placeholder; implemented in Phase 1." >&2
exit 1
