"""Placeholder — ``LlamaClient``, implemented in Phase 1.

OpenAI-compatible ``/v1/chat/completions`` calls against llama-server, including
streaming. Endpoints must never call this directly; they go through TutorEngine
(Phase 2).

No logic yet by design: Phase 0 must not contain business logic.
See ``ENGINEERING_PLAN.md`` §5.
"""
