"""Placeholder — conversation history window, implemented in Phase 8.

Recent turns are kept verbatim (``memory.recent_turns``); older turns are
compressed into a structured JSON summary. Never grow the window just because the
context window is large.

No logic yet by design: Phase 0 must not contain business logic.
See ``ENGINEERING_PLAN.md`` §10.
"""
