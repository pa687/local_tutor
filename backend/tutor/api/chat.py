"""Placeholder — chat endpoints, implemented in Phase 1 and Phase 6.

Phase 1 adds ``POST /api/chat`` with SSE streaming (§5, §16). Phase 6 adds
``POST /api/chat/image`` (multipart, transcription before solving, §8).

No logic yet by design: Phase 0 must not contain business logic.
See ``ENGINEERING_PLAN.md`` §5, §8, §16.
"""
