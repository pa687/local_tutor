"""Placeholder — ``TutorEngine``, implemented in Phase 2.

Call chain: ``ChatRequest → TutorEngine → Policy → ContextBuilder → LLMClient →
TutorResponse``. Endpoints must never call ``LlamaClient`` directly.

No logic yet by design: Phase 0 must not contain business logic.
See ``ENGINEERING_PLAN.md`` §6.
"""
