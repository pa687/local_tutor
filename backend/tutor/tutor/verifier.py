"""Placeholder — verification loop, implemented in Phase 4.

Extracts verifiable claims from an answer, checks them with SymPy, and asks the
model to re-check on conflict, at most ``tutor.max_verification_attempts`` times.
Conflicts must never be hidden.

No logic yet by design: Phase 0 must not contain business logic.
See ``ENGINEERING_PLAN.md`` §7, §22.
"""
