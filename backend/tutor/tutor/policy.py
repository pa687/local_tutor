"""Placeholder — Tutor Policy layer, implemented *in full* in Phase 2.

Phase 2 builds the complete rule layer (§12: five tutor rules + grade constraints);
Phase 10 only extends it with new modes. This module must never be left as a stub
that silently allows policy to leak into system prompts.

No logic yet by design: Phase 0 must not contain business logic.
See ``ENGINEERING_PLAN.md`` §12.
"""
