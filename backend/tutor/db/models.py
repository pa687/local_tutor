"""Placeholder — database models, implemented in Phase 7.

``StudentProfile`` and ``KnowledgeState`` tables (§9). Note: ``KnowledgeState.confidence``
(how sure we are about mastery) is a different concept from ``TutorResponse.confidence``
(how trustworthy this answer is) and must not share a field, enum or update path.

No logic yet by design: Phase 0 must not contain business logic.
See ``ENGINEERING_PLAN.md`` §9.
"""
